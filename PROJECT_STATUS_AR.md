# حالة مشروع Clinico / ClinicOS

آخر تحديث: 20 أغسطس 2026  
المشروع المعتمد: `C:\Users\Administrator\Downloads\clinicos`

## 1. فكرة المنتج

Clinico هو نظام تشغيل وCRM للعيادات والمراكز الطبية، وليس مجرد نموذج لإضافة أسماء مرضى.

الهدف المباشر هو تنظيم دخول المرضى وبياناتهم واحتفاظ كل عيادة بقاعدة بيانات مستقلة وآمنة. كما يتيح، عند وجود موافقة تسويقية صريحة من المريض، بناء جماهير تسويقية قانونية لاستخدامها لاحقًا مع منصات الإعلان مثل Meta وTikTok وSnapchat.

القيمة التي يقدمها للعيادات التي لديها CRM قديم هي نقل قاعدة المرضى دفعة واحدة بدل إدخالها اسمًا اسمًا، ثم تنظيمها والبحث فيها وتقسيمها تسويقيًا بشكل أكثر فائدة.

## 2. المعمارية المعتمدة

- Monorepo باستخدام `pnpm`.
- الواجهة: Next.js 14.
- الـAPI: NestJS.
- قاعدة البيانات: PostgreSQL.
- ORM: Prisma 5.22.0.
- Redis وPostgreSQL يعملان عبر Docker Compose.
- المصادقة: JWT مع صلاحيات حسب الدور.
- عزل البيانات: كل طلب مرضى مرتبط بـ`organizationId`، لذلك لا يرى مركز بيانات مركز آخر.

لا توجد عودة إلى مشروع Vite أو Bolt القديم.

## 3. ما تم تنفيذه والتحقق منه

### التشغيل والبنية

- تشغيل Docker/PostgreSQL/Redis والتحقق من الاتصال.
- تشغيل NestJS API والتحقق من Health endpoint.
- تشغيل Next.js frontend.
- Prisma generation والمهاجرات تعمل.
- Type checking وAPI production build وWeb production build نجحت خلال العمل.

### الحسابات والأمان

- تسجيل دخول JWT يعمل.
- أدوار Owner وDoctor وReceptionist وصلاحيات المرضى موجودة.
- جلسة كل مستخدم مرتبطة بمركزه فقط.
- إن لم توجد جلسة، تنتقل صفحات المرضى إلى صفحة الدخول بدل ظهور رسالة Unauthorized فوق النموذج.

بيانات التطوير الحالية:

| الحقل | القيمة |
| --- | --- |
| Clinic code | `dev-clinic` |
| Owner email | `owner@dev.local` |
| Password | `dev_password_123` |

### سجل المرضى والدخول

- إنشاء وتعديل وعرض وبحث المرضى.
- توليد MRN تلقائيًا.
- حقول الإدخال: الاسم، الهاتف، تاريخ الميلاد، تاريخ الدخول، النوع، الحالة الاجتماعية، الوظيفة، المحافظة، المدينة، مصدر العميل، والموافقة التسويقية.
- تاريخ الدخول يُخزَّن كبيان مستقل.
- قوائم مبدئية لمحافظتي القاهرة والجيزة ومدنهما في نموذج الإدخال.
- سجل طبي/Clinical records موجود من المرحلة السابقة.

### ترحيل البيانات من CRM قديم

صفحة النقل: `http://localhost:3000/patients/import`

- قراءة ملفات CSV ومعاينة أول 8 سجلات قبل الحفظ.
- تنزيل قالب CSV جاهز من الصفحة.
- دعم عناوين إنجليزية وعربية شائعة، ومنها: `الاسم الكامل`، `رقم الهاتف`، `النوع`، `المحافظة`، `المنطقة`، `الجامعة`، و`تاريخ الميلاد`.
- يدعم حتى 300,000 سجل في الملف الواحد.
- ينقل الملف داخليًا على دفعات 1,000 سجل مع مؤشر تقدم.
- حد طلب الـAPI رُفع بأمان إلى 5MB لكي لا يفشل استيراد الدفعة برسالة `request entity too large`.
- يمنع تكرار رقم الهاتف داخل نفس المركز فقط، ولا يخلط مراكز مختلفة.
- تم اختبار نقل ملف 2,000 سجل بنجاح.
- العدد المؤكد للمرضى بعد الاختبار: 2,009 سجلات (2,000 مستورد + سجلات سابقة).

## 4. الوضع الحالي وطريقة التشغيل

لتشغيل المشروع محليًا:

```powershell
cd C:\Users\Administrator\Downloads\clinicos
docker compose up -d
pnpm dev
```

بعد ظهور الرسائل التالية، يكون النظام جاهزًا:

- API: `ClinicOS API running on http://0.0.0.0:3001`
- Web: `http://localhost:3000`

يجب إبقاء نافذة PowerShell مفتوحة أثناء التجربة. إغلاقها يوقف الواجهة والـAPI؛ كما يجب إبقاء Docker Desktop قيد التشغيل لقاعدة البيانات.

## 5. نقطة التوقف الحالية

انتهت مرحلة Admission CRM + CSV Migration بنجاح، ثم أضيف مركز الجمهور التسويقي وإدارة فريق العيادة.

الخطوة التالية المعتمدة هي **المواعيد والزيارات**: تنظيم الحجز والمتابعة لكل مريض داخل العيادة.

### تحديث 21 أغسطس 2026

- أضيفت صفحة `Team` لإدارة أعضاء فريق العيادة.
- المالك أو المدير المخول يستطيع إنشاء حساب `Admin` أو `Doctor` أو `Receptionist` داخل مركزه فقط.
- كلمة مرور عضو الفريق يجب أن تكون 10 أحرف على الأقل وتحتوي حرفًا ورقمًا؛ يسلّمها المدير للموظف عبر قناة آمنة.
- أُضيفت ترويسات أمان HTTP أساسية ورفض تشغيل الإنتاج إذا كانت أسرار JWT ضعيفة أو كان عنوان الواجهة غير HTTPS.
- أضيف دليل النشر: `DEPLOYMENT_READINESS_AR.md` وقالب الأسرار `PRODUCTION_ENV_TEMPLATE.md`.

## 6. ما تبقى بالترتيب

### أولوية 1: مركز الجمهور التسويقي

- فلاتر: المحافظة، المدينة، النوع، العمر، مصدر العميل، وتاريخ الدخول.
- إظهار عدد المرضى المطابقين فقط.
- يشمل تلقائيًا المرضى الذين لديهم `marketingConsent = true` فقط.
- تصدير CSV مخصص للاستخدام مع منصات الإعلانات.
- سجل audit يوضح من صدّر بيانات ومتى ومع أي فلاتر.

### أولوية 2: ربط منصات الإعلان

- البدء بـMeta Custom Audiences ثم TikTok ثم Snapchat.
- هذا الجزء يحتاج حسابات مطور وتطبيقات OAuth ومفاتيح وصلاحيات رسمية من كل منصة.
- لا يجب ادعاء وجود ربط مباشر قبل تجهيز تلك الاعتمادات والمراجعات القانونية اللازمة.

### أولوية 3: تجربة المنتج والواجهة

- Landing page احترافية لبيع المنتج.
- واجهة عربية كاملة، مع دعم English وRTL/LTR.
- لوحة إحصاءات: المرضى، المدن، مصادر الإحالة، والموافقات التسويقية.
- توحيد المدن والمحافظات في الإدخال والتعديل.

### أولوية 4: جاهزية SaaS

- Onboarding لإنشاء عيادة وحساب Owner جديد بأمان.
- واجهات إدارة المستخدمين والأدوار للمركز.
- إعدادات المركز والفروع.
- CI/CD وGitHub Actions وتوثيق الإنتاج والنسخ الاحتياطي.

### أولوية 5: وظائف طبية وتشغيلية

- المواعيد والزيارات.
- متابعة الحالات والسجل الطبي المنظم.
- تقارير وتشغيل وإشعارات.

## 7. ملاحظات مهمة

- لا تحذف أو تعيد تهيئة قاعدة البيانات من أجل التجربة.
- لا تستخدم بيانات مرضى حقيقية في ملف اختبار أو في سجل تسويقي من دون الموافقة الصريحة والالتزام بالقوانين المعمول بها.
- المشروع الصحيح الوحيد للعمل هو المسار الموجود في Downloads أعلاه؛ لا تستخدم النسخ القديمة الموجودة في مجلدات Codex.
