# نشر Clinico: دليل الجاهزية

## ما الذي يُنشر؟

Clinico ليس ملف HTML يُرفع لموقع عادي. يُشغّل كأربع خدمات منفصلة:

1. واجهة Next.js على `app.yourdomain.com`.
2. API NestJS على `api.yourdomain.com` أو خدمة داخلية.
3. PostgreSQL مُدار ودائم للبيانات.
4. Redis مُدار عند تفعيل وظائف الطوابير والكاش.

لا تستخدم قاعدة Docker المحلية أو بيانات `dev-clinic` في الإنتاج.

## بدون ميزانية الآن

- استمر في التجربة محليًا عبر `docker compose up -d` ثم `pnpm dev`.
- لا ترفع بيانات مرضى حقيقية إلى خدمة مجانية أو رابط عام مؤقت.
- لا تفتح منفذ جهازك للإنترنت من أجل تجربة العملاء.

## قبل أول نشر فعلي

- [ ] رفع الكود إلى مستودع GitHub خاص.
- [ ] شراء نطاق وربطه بـHTTPS.
- [ ] إنشاء Postgres مُدار مع نسخ احتياطي وتفعيل الاسترجاع.
- [ ] إعداد الأسرار في لوحة الاستضافة من `PRODUCTION_ENV_TEMPLATE.md`، لا داخل Git.
- [ ] تشغيل `prisma migrate deploy` قبل نشر API.
- [ ] إنشاء أول مركز وحساب مالك بكلمة مرور فريدة؛ عدم استخدام حسابات التطوير.
- [ ] تفعيل مزود بريد لاسترجاع كلمة المرور ودعوات الفريق.
- [ ] إضافة rate limit ومراقبة أخطاء وuptime وaudit log.
- [ ] مراجعة سياسة الخصوصية والموافقات التسويقية مع مختص قانوني.

## مسار اقتصادي لاحقًا

ابدأ بخطة مدفوعة صغيرة لدى مزود واحد مثل Render: Web service للواجهة، Web/Private service للـAPI، وManaged PostgreSQL. لا تعتبر الخطة المجانية بيئة إنتاج للبيانات الطبية.
