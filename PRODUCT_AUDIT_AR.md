# مراجعة المنتج — Clinico

تاريخ المراجعة: 23 أغسطس 2026

## ما يعمل الآن

- عزل كامل للبيانات حسب العيادة عبر `organizationId`.
- تسجيل مرضى، بحث، رقم ملف تلقائي، استيراد CSV، وموافقة تسويقية منفصلة.
- مواعيد وحالات حضور، مع صلاحيات تمنع الاستقبال من فتح التاريخ الطبي.
- سجل طبي زمني للطبيب والفريق الطبي فقط.
- أدوار Owner / Admin / Doctor / Receptionist وصلاحيات API من الخادم.
- لوحة صاحب المنصة `/platform` للعيادات والاشتراكات، ولا تعرض بيانات طبية.
- تجربة مجانية 14 يومًا، تسجيل عيادة، واستعادة/إدارة أعضاء الفريق.
- تسجيل دخول Google لحساب سبق دعوته إلى نفس العيادة.
- وصفة إلكترونية قابلة للطباعة، وترويسة مخصصة من الإعدادات.

## تجربة كل دور

### الاستقبال

يبدأ من لوحة الاستقبال ثم يستخدم فقط:

1. تسجيل أو البحث عن مريض.
2. حجز موعد أو تغيير حالة الحضور.
3. فتح بيانات الاستقبال للمريض.

لا يملك API لقراءة السجل الطبي أو الوصفات أو تصدير الجمهور التسويقي.

### الطبيب

يبدأ من المواعيد ثم يفتح ملف المريض، ويملك:

1. مراجعة التاريخ الطبي.
2. إضافة ملاحظة أو تشخيص أو خطة علاج.
3. إنشاء وصفة إلكترونية وحفظها وطباعتها.

### مالك العيادة

يرى مؤشرات العيادة، يضيف الفريق، يستورد CRM، يضبط الترويسة، ويتحكم في المواعيد والجمهور التسويقي.

### مالك المنصة

يدخل إلى `/platform` بحساب عليه `isPlatformAdmin=true`. هذه الصفحة تفصل إدارة الاشتراكات عن بيانات العيادات الطبية.

## عناصر لا يجوز اعتبارها مكتملة بعد

- حسابات مالية حقيقية: رسوم كشف، مدفوعات، إيصالات، صندوق، وتقارير يومية.
- استضافة API وقاعدة بيانات إنتاجية؛ واجهة Vercel الحالية هي عرض آمن فقط ولا يجب أن تتصل ببيانات مرضى حقيقية.
- رفع ملفات/صور للمريض أو لوجو داخل تخزين آمن؛ رابط اللوجو الحالي اختياري حتى يتم ربط تخزين ملفات.
- نظام مواعيد متقدم: أوقات عمل الطبيب، منع التعارض، رسائل تذكير واتساب/SMS.
- تقارير تشغيلية ومالية، نسخ احتياطي مجدول، وسياسة احتفاظ وحذف بيانات.
- مراجعة قانونية للخصوصية والموافقات الطبية والتسويقية في الدولة المستهدفة.

## ترتيب الإطلاق الصحيح

1. عرض نسخة Demo ببيانات وهمية فقط.
2. قبل أول عيادة فعلية: استضافة API + PostgreSQL إنتاجي + نسخ احتياطي.
3. بناء المالية والإيصالات قبل استخدام موظف استقبال مع مدفوعات حقيقية.
4. تجربة مع عيادتين صغيرتين، ثم تثبيت الأسعار وخطة الدعم.
